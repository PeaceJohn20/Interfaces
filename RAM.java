public class RAM {
    String name = "DDR4";
    int readSpeed = 3;
    int writeSpeed = 5;

    @Override
    public String toString() {
        return "RAM{" + "name='" + name + '\'' + ", readSpeed=" + readSpeed + ", writeSpeed=" + writeSpeed + '}';
    }

}